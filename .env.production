# Baked into release builds (npm run build / build:installer).
# Set VITE_PORTAL_API_TOKEN in .env or CI — must match portal-api PORTAL_BEARER_TOKEN.
VITE_PORTAL_API_URL=https://sepela-erp-api.onrender.com
