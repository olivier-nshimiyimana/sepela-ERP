# Portal API connection (same bearer token as portal-api PORTAL_BEARER_TOKEN)
# Local dev: run `npm run dev` in portal-api/, then use:
VITE_PORTAL_API_URL=http://127.0.0.1:4000
# Production (uncomment for installer builds against Render):
# VITE_PORTAL_API_URL=https://sepela-erp-api.onrender.com
VITE_PORTAL_API_TOKEN=thisismyturnmotherFucker@1
